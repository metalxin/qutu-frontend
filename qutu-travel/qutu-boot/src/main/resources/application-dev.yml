spring:
  cache:
    type: none      # 改成 none，彻底解决 500
  cloud:
    openfeign:
      client:
        config:
          qutu-upms-biz:
            url: http://127.0.0.1:${server.port}${server.servlet.context-path}
          qutu-auth:
            url: http://127.0.0.1:${server.port}${server.servlet.context-path}
    discovery:
      client:
        simple:
          instances:
            qutu-upms-biz:
              - uri: http://127.0.0.1:${server.port}${server.servlet.context-path}
            qutu-auth:
              - uri: http://127.0.0.1:${server.port}${server.servlet.context-path}
  quartz:
    properties:
      org:
        quartz:
          jobStore:
            tablePrefix: qrtz_
  data:
    redis:
      database: 5
      host: localhost
  # 数据库相关配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql://127.0.0.1:3306/qutu?characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=GMT%2B8&allowMultiQueries=true&nullCatalogMeansCurrent=true
  # 邮件配置（请替换为你的SMTP信息）
  mail:
    host: smtp.qq.com
    port: 465
    username: 2770496921@qq.com
    password: kwyqklqdciwddfgc
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory
          starttls:
            enable: true
            required: true
    protocol: smtps


# 本地文件系统
file:
  local:
    enable: true
    base-path: /Users/goldxin/workspace/qutu/file/upload

## 登录配置
security:
  encodeKey: thanks,pig4cloud
  ignore-clients:
    - test

# 配置文件加密
jasypt:
  encryptor:
    password: pig
    algorithm: PBEWithMD5AndDES
    iv-generator-classname: org.jasypt.iv.NoIvGenerator

# swagger 配置（修正）
swagger:
#  token-url: ${swagger.gateway}/admin/oauth2/token
  token-url: /oauth2/token   # 👈 单体版正确路径

# AI 大模型配置
qutu:
  ai:
    base-url: http://127.0.0.1:8000/v1
    api-key: omlx
    model: Qwen3.5-4B-MLX-4bit
    timeout: 180
    max-tokens: 2048
    temperature: 0.7